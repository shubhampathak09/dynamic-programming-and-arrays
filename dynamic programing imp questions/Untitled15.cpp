// algo to check if bbst
/*
bool checkbst(struct node*root)
{
	
	if(root==NULL)
	return 1;
	
	
	if(root->left&&mx_val(root->left)>root->data)
	return 0;
	
	if(root->right&&mn_val(root->right)<root->data)
	return 0;
	
	if(checkbst(root->left)==0||checkbst(root->right)==0)
	return 0;
	
	return 1;
	
}
*/

// structre of bst

// insertnode in bst

// function for min value in bst


// function for max value in bst


// impl 

//pass some test cases

//----

----------------------------------------------------------------------------------------------------


